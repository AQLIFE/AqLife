info:
  name: delete /api/Todo
  type: http
  seq: 4
  tags:
    - Todo

http:
  method: DELETE
  url: "{{baseUrl}}/api/Todo"
  body:
    type: json
    data: |-
      {
        "uid": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Todo"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
