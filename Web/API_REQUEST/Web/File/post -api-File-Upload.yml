info:
  name: post /api/File/Upload
  type: http
  seq: 6
  tags:
    - File

http:
  method: POST
  url: "{{baseUrl}}/api/File/Upload"
  body:
    type: multipart-form
    data:
      - name: File
        type: text
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Upload"
      method: POST
      body:
        type: multipart-form
        data:
          - name: File
            type: text
            value: ""
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: |-
          [
            ""
          ]
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Upload"
      method: POST
      body:
        type: multipart-form
        data:
          - name: File
            type: text
            value: ""
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            ""
          ]
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Upload"
      method: POST
      body:
        type: multipart-form
        data:
          - name: File
            type: text
            value: ""
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: |-
          [
            ""
          ]
