info:
  name: patch /api/File/tag
  type: http
  seq: 7
  tags:
    - File

http:
  method: PATCH
  url: "{{baseUrl}}/api/File/tag"
  body:
    type: json
    data: |-
      {
        "uid": "",
        "tags": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/tag"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "uid": "",
            "tags": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/tag"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "uid": "",
            "tags": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/tag"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "uid": "",
            "tags": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: ""
