info:
  name: delete /api/File
  type: http
  seq: 3
  tags:
    - File

http:
  method: DELETE
  url: "{{baseUrl}}/api/File"
  body:
    type: json
    data: |-
      {
        "uid": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
