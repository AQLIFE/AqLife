info:
  name: post /api/File/Publish
  type: http
  seq: 8
  tags:
    - File

http:
  method: POST
  url: "{{baseUrl}}/api/File/Publish"
  body:
    type: json
    data: |-
      {
        "uid": "dbe0c64a-7e12-44fe-89f8-cd92cbcaf4d7"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: baseUrl
      value: 192.168.0.100:5110

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Publish"
      method: POST
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Publish"
      method: POST
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/Publish"
      method: POST
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: ""
