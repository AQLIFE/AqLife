info:
  name: patch /api/File/CancelSchedule
  type: http
  seq: 11
  tags:
    - File

http:
  method: PATCH
  url: "{{baseUrl}}/api/File/CancelSchedule"
  body:
    type: json
    data: |-
      {
        "uid": "f7aa5670-3b5e-4c39-9d3b-d1214ad92e7c"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/File/CancelSchedule"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
