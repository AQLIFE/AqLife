info:
  name: delete /api/Tag
  type: http
  seq: 4
  tags:
    - Tag

http:
  method: DELETE
  url: "{{baseUrl}}/api/Tag"
  body:
    type: json
    data: |-
      {
        "uid": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Tag"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "uid": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
