info:
  name: delete /api/Account
  type: http
  seq: 3
  tags:
    - Account

http:
  method: DELETE
  url: "{{baseUrl}}/api/Account"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account"
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
