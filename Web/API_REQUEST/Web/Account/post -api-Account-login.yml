info:
  name: post /api/Account/login
  type: http
  seq: 4
  tags:
    - Account

http:
  method: POST
  url: "{{baseUrl}}/api/Account/login"
  body:
    type: json
    data: |-
      {
        "accountName": "",
        "secretKey": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "accountName": "",
            "secretKey": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "accountName": "",
            "secretKey": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "accountName": "",
            "secretKey": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: ""
