info:
  name: patch /api/Account/avatar
  type: http
  seq: 5
  tags:
    - Account

http:
  method: PATCH
  url: "{{baseUrl}}/api/Account/avatar"
  body:
    type: multipart-form
    data:
      - name: avatar
        type: file
        value: []
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/avatar"
      method: PATCH
      body:
        type: multipart-form
        data:
          - name: avatar
            type: file
            value: []
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/avatar"
      method: PATCH
      body:
        type: multipart-form
        data:
          - name: avatar
            type: file
            value: []
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Account/avatar"
      method: PATCH
      body:
        type: multipart-form
        data:
          - name: avatar
            type: file
            value: []
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: ""
